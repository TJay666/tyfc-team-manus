# Django settings
SECRET_KEY=change-me
DEBUG=0
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (PostgreSQL in docker-compose)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=tyfc
DB_USER=tyfc
DB_PASSWORD=tyfc
DB_HOST=db
DB_PORT=5432

# Static / Media placeholders (optional)
